[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Redirect all routes to index.html — required for React Router / client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200